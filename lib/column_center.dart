import 'package:dice_roll_app/image_button.dart';
import 'package:flutter/material.dart';

class ColumnCenter extends StatelessWidget {
  const ColumnCenter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: ImageButton()
    );
  }
}
